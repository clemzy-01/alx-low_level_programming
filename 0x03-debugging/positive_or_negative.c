#include "main.h"

/**
 * positve_negative - main function
 *@i: integer to check
 *
 * Return: always 0
 */
 
void positive_or_negative(int i)
{
	/*srand(time(0));
	i = rand() - RAND_MAX / 2;*/

	printf("%d is zero\n", i);
}
